#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>
#include "main.h"
/**
*_islower - checks the lower chars
*/
int _islower(int c);
{
	/* your code goes there */
	char out;

	if(out >= 'a' && out <= 'z')
	{
		return(1);
	}
	else 
	{
		return(0);
	}
}

